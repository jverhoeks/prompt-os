{
  "format_version": 1,
  "tools": [
    {"name": "system.now", "block": "clock", "purpose": "Return local date and time from the configured clock.", "mutates": false},
    {"name": "store.put", "block": "documents", "purpose": "Create or replace an application-scoped JSON document.", "mutates": true},
    {"name": "store.get", "block": "documents", "purpose": "Read one application-scoped document.", "mutates": false},
    {"name": "store.query", "block": "documents", "purpose": "Find documents using exact JSON field matches.", "mutates": false},
    {"name": "store.search", "block": "retrieval", "purpose": "Find relevant documents using deterministic text matching.", "mutates": false},
    {"name": "store.scan", "block": "documents", "purpose": "Read recent documents for structure discovery and review.", "mutates": false},
    {"name": "store.archive", "block": "documents", "purpose": "Soft-delete a document while retaining history.", "mutates": true},
    {"name": "store.history", "block": "documents", "purpose": "Read the append-only revisions of one document.", "mutates": false},
    {"name": "store.describe", "block": "documents", "purpose": "Return collection counts and the current generated contract.", "mutates": false},
    {"name": "store.aggregate", "block": "aggregation", "purpose": "Calculate deterministic counts and sums over stored documents.", "mutates": false},
    {"name": "contract.current", "block": "data-contract", "purpose": "Read the current promoted application data contract.", "mutates": false},
    {"name": "contract.evidence", "block": "data-contract", "purpose": "List immutable evidence available to support a contract candidate.", "mutates": false},
    {"name": "contract.propose", "block": "data-contract", "purpose": "Submit an evidence-backed generated contract candidate.", "mutates": true},
    {"name": "math.evaluate", "block": "calculation", "purpose": "Evaluate bounded arithmetic deterministically.", "mutates": false},
    {"name": "math.sample", "block": "calculation", "purpose": "Sample a univariate expression over a closed interval for a continuous plot.", "mutates": false},
    {"name": "unit.convert", "block": "conversion", "purpose": "Convert compatible measurements deterministically.", "mutates": false},
    {"name": "view.present", "block": "presentation", "purpose": "Return a generic structured view description for capable clients.", "mutates": false}
  ]
}
